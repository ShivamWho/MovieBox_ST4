import React from "react";

const base = (props) => {
 
    return(
        <>
            <div className="base">
                <span> {props.moviename} </span>    
            </div>
        </>
    );
}
export default base;
